/*
template<typename T>
class MyTemp 
{
   public:   
      MyTemp() {};              
      T getValue();
   private:      
      T var;
};

template <typename T>
____________ getValue() #right answer is T MyTemp<T>::
{      
   return var;
}
*/
/*
In ____, one or more members of a class are objects of another class type.
composition
When defining a class, typically all data members are private while member functions are public.
true
An exception that is thrown may be caught by multiple catch blocks
false
runtime_error will create a string equal to the argument passed to the function.
false
*/